import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Portal PKL SMK HASSINA — served di portal.smkhassina.sch.id
  // Saat deploy ke CloudPanel: build output .next di-sync ke /home/portal/htdocs/portal.smkhassina.sch.id
};

export default nextConfig;
